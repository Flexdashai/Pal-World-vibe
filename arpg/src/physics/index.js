/** STUB — replaced by the physics agent. See ../../ARCHITECTURE.md. */
export class PhysicsSystem {
  static id = 'physics';
  static deps = [];
  async init(ctx) { this.ctx = ctx; }
  dispose() {}
}
