/** STUB — replaced by the materials agent. See ../../ARCHITECTURE.md. */
export class MaterialSystem {
  static id = 'materials';
  static deps = [];
  async init(ctx) { this.ctx = ctx; }
  dispose() {}
}
